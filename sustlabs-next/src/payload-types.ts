/* tslint:disable */
/* eslint-disable */
/**
 * This file was automatically generated by Payload.
 * DO NOT MODIFY IT BY HAND. Instead, modify your source Payload config,
 * and re-run `payload generate:types` to regenerate this file.
 */

/**
 * Supported timezones in IANA format.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "supportedTimezones".
 */
export type SupportedTimezones =
  | 'Pacific/Midway'
  | 'Pacific/Niue'
  | 'Pacific/Honolulu'
  | 'Pacific/Rarotonga'
  | 'America/Anchorage'
  | 'Pacific/Gambier'
  | 'America/Los_Angeles'
  | 'America/Tijuana'
  | 'America/Denver'
  | 'America/Phoenix'
  | 'America/Chicago'
  | 'America/Guatemala'
  | 'America/New_York'
  | 'America/Bogota'
  | 'America/Caracas'
  | 'America/Santiago'
  | 'America/Buenos_Aires'
  | 'America/Sao_Paulo'
  | 'Atlantic/South_Georgia'
  | 'Atlantic/Azores'
  | 'Atlantic/Cape_Verde'
  | 'Europe/London'
  | 'Europe/Berlin'
  | 'Africa/Lagos'
  | 'Europe/Athens'
  | 'Africa/Cairo'
  | 'Europe/Moscow'
  | 'Asia/Riyadh'
  | 'Asia/Dubai'
  | 'Asia/Baku'
  | 'Asia/Karachi'
  | 'Asia/Tashkent'
  | 'Asia/Calcutta'
  | 'Asia/Dhaka'
  | 'Asia/Almaty'
  | 'Asia/Jakarta'
  | 'Asia/Bangkok'
  | 'Asia/Shanghai'
  | 'Asia/Singapore'
  | 'Asia/Tokyo'
  | 'Asia/Seoul'
  | 'Australia/Brisbane'
  | 'Australia/Sydney'
  | 'Pacific/Guam'
  | 'Pacific/Noumea'
  | 'Pacific/Auckland'
  | 'Pacific/Fiji';

export interface Config {
  auth: {
    users: UserAuthOperations;
  };
  blocks: {};
  collections: {
    products: Product;
    'legal-pages': LegalPage;
    leads: Lead;
    media: Media;
    documents: Document;
    users: User;
    'payload-kv': PayloadKv;
    'payload-locked-documents': PayloadLockedDocument;
    'payload-preferences': PayloadPreference;
    'payload-migrations': PayloadMigration;
  };
  collectionsJoins: {};
  collectionsSelect: {
    products: ProductsSelect<false> | ProductsSelect<true>;
    'legal-pages': LegalPagesSelect<false> | LegalPagesSelect<true>;
    leads: LeadsSelect<false> | LeadsSelect<true>;
    media: MediaSelect<false> | MediaSelect<true>;
    documents: DocumentsSelect<false> | DocumentsSelect<true>;
    users: UsersSelect<false> | UsersSelect<true>;
    'payload-kv': PayloadKvSelect<false> | PayloadKvSelect<true>;
    'payload-locked-documents': PayloadLockedDocumentsSelect<false> | PayloadLockedDocumentsSelect<true>;
    'payload-preferences': PayloadPreferencesSelect<false> | PayloadPreferencesSelect<true>;
    'payload-migrations': PayloadMigrationsSelect<false> | PayloadMigrationsSelect<true>;
  };
  db: {
    defaultIDType: number;
  };
  fallbackLocale: null;
  globals: {
    home: Home;
    'smart-db': SmartDb;
    fms: Fm;
    'ohm-os': OhmO;
    ora: Ora;
    solutions: Solution;
    support: Support;
    navigation: Navigation;
    settings: Setting;
  };
  globalsSelect: {
    home: HomeSelect<false> | HomeSelect<true>;
    'smart-db': SmartDbSelect<false> | SmartDbSelect<true>;
    fms: FmsSelect<false> | FmsSelect<true>;
    'ohm-os': OhmOsSelect<false> | OhmOsSelect<true>;
    ora: OraSelect<false> | OraSelect<true>;
    solutions: SolutionsSelect<false> | SolutionsSelect<true>;
    support: SupportSelect<false> | SupportSelect<true>;
    navigation: NavigationSelect<false> | NavigationSelect<true>;
    settings: SettingsSelect<false> | SettingsSelect<true>;
  };
  locale: null;
  widgets: {
    collections: CollectionsWidget;
  };
  user: User;
  jobs: {
    tasks: unknown;
    workflows: unknown;
  };
}
export interface UserAuthOperations {
  forgotPassword: {
    email: string;
    password: string;
  };
  login: {
    email: string;
    password: string;
  };
  registerFirstUser: {
    email: string;
    password: string;
  };
  unlock: {
    email: string;
    password: string;
  };
}
/**
 * One page per hardware product.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "products".
 */
export interface Product {
  id: number;
  /**
   * How this product appears in the Products menu, e.g. "o3 - Energy Visibility".
   */
  navLabel: string;
  /**
   * The last part of the address, with no slashes — "o3-energy-visibility" puts the page at sustlabs.com/products/o3-energy-visibility. Changing this breaks any existing links to the page.
   */
  slug: string;
  /**
   * Lower numbers appear first in the menu.
   */
  order: number;
  /**
   * The small label used across the page, e.g. "O3 MONITORING".
   */
  eyebrow: string;
  /**
   * The plain-language product statement.
   */
  title: string;
  description: string;
  introSection: {
    /**
     * The product name line above the heading.
     */
    titleKicker?: string | null;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * Each row becomes its own paragraph, in this order.
     */
    descriptions?:
      | {
          text: string;
          id?: string | null;
        }[]
      | null;
    /**
     * One of the fixed design colours. Pick from the list — do not type a colour code.
     */
    accent:
      | 'var(--terra500)'
      | 'var(--teal500)'
      | 'var(--success500)'
      | 'var(--black300)'
      | 'var(--black400)'
      | 'var(--black500)'
      | 'var(--white)'
      | '#145599'
      | '#4CAF53';
  };
  heroSection: {
    /**
     * The small line above the hero heading.
     */
    kicker: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * Optional last fragment, shown after the highlighted half in the normal colour.
     */
    titleRest?: string | null;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * Colour of the highlighted words in the hero heading.
     */
    accentColor:
      | 'var(--terra500)'
      | 'var(--teal500)'
      | 'var(--success500)'
      | 'var(--black300)'
      | 'var(--black400)'
      | 'var(--black500)'
      | 'var(--white)'
      | '#145599'
      | '#4CAF53';
    /**
     * The buttons shown in this section, left to right.
     */
    actions?:
      | {
          /**
           * The text on the button.
           */
          label: string;
          /**
           * Where it goes. Start with "/" for a page on this site (e.g. /support), "/#" for a section of the homepage, or a full https:// address. Links used in many places — the demo booking link, the store — live under Site settings, so change them there once instead of here.
           */
          href: string;
          /**
           * Primary is the filled button. Use it once per section.
           */
          variant: 'primary' | 'secondary';
          /**
           * Tick this to open the "Explore interest" pop-up form instead of following the link.
           */
          opensLeadForm?: boolean | null;
          /**
           * Used to group clicks on this button in Google Analytics. Set once and then leave it alone — changing it splits the reporting for this button in two.
           */
          analyticsId?: string | null;
          id?: string | null;
        }[]
      | null;
    /**
     * The product photograph beside the hero text.
     */
    image?: (number | null) | Media;
    /**
     * Describes the image for screen readers. Leave empty to use the description saved on the image itself in the Images library.
     */
    alt?: string | null;
    /**
     * Controls how the photo is cropped and positioned. This changes the layout, not the photo.
     */
    imageVariant?: ('o3' | 'o4' | 'smart-db') | null;
  };
  featureSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * One of the fixed design colours. Pick from the list — do not type a colour code.
     */
    accent:
      | 'var(--terra500)'
      | 'var(--teal500)'
      | 'var(--success500)'
      | 'var(--black300)'
      | 'var(--black400)'
      | 'var(--black500)'
      | 'var(--white)'
      | '#145599'
      | '#4CAF53';
    /**
     * Which half of the heading gets the highlight colour above.
     */
    accentTarget?: ('accent' | 'lead') | null;
    /**
     * On by default. Turn it off to keep the whole heading on one line. This changes the layout, not the wording.
     */
    breakAfterLead?: boolean | null;
    cards?:
      | {
          /**
           * The small label above the card heading, e.g. "Live Usage".
           */
          eyebrow: string;
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
  };
  useCaseSection: {
    /**
     * Turns the use-case section on or off on the live site. The wording is kept either way, so you can switch it back on later without retyping anything.
     */
    enabled?: boolean | null;
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow?: string | null;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead?: string | null;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent?: string | null;
    /**
     * Optional paragraph under the heading. Leave empty to show none.
     */
    description?: string | null;
    /**
     * One of the fixed design colours. Pick from the list — do not type a colour code.
     */
    accent:
      | 'var(--terra500)'
      | 'var(--teal500)'
      | 'var(--success500)'
      | 'var(--black300)'
      | 'var(--black400)'
      | 'var(--black500)'
      | 'var(--white)'
      | '#145599'
      | '#4CAF53';
    /**
     * Which half of the heading gets the highlight colour above.
     */
    accentTarget?: ('accent' | 'lead') | null;
    /**
     * On by default. Turn it off to keep the whole heading on one line. This changes the layout, not the wording.
     */
    breakAfterLead?: boolean | null;
    cards?:
      | {
          /**
           * Shown as the card ordinal. Usually "01." — type it exactly as it should appear, including the full stop.
           */
          number: string;
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
  };
  /**
   * A second block lower down the page. Only o4 uses this today.
   */
  postDetailUseCaseSection: {
    /**
     * Turns the second use-case block on or off on the live site. The wording is kept either way, so you can switch it back on later without retyping anything.
     */
    enabled?: boolean | null;
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow?: string | null;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead?: string | null;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent?: string | null;
    /**
     * One of the fixed design colours. Pick from the list — do not type a colour code.
     */
    accent:
      | 'var(--terra500)'
      | 'var(--teal500)'
      | 'var(--success500)'
      | 'var(--black300)'
      | 'var(--black400)'
      | 'var(--black500)'
      | 'var(--white)'
      | '#145599'
      | '#4CAF53';
    /**
     * Which half of the heading gets the highlight colour above.
     */
    accentTarget?: ('accent' | 'lead') | null;
    /**
     * On by default. Turn it off to keep the whole heading on one line. This changes the layout, not the wording.
     */
    breakAfterLead?: boolean | null;
    cards?:
      | {
          /**
           * Shown as the card ordinal. Usually "01." — type it exactly as it should appear, including the full stop.
           */
          number: string;
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
  };
  ctaSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * One of the fixed design colours. Pick from the list — do not type a colour code.
     */
    accent:
      | 'var(--terra500)'
      | 'var(--teal500)'
      | 'var(--success500)'
      | 'var(--black300)'
      | 'var(--black400)'
      | 'var(--black500)'
      | 'var(--white)'
      | '#145599'
      | '#4CAF53';
    /**
     * Which half of the heading gets the highlight colour above.
     */
    accentTarget?: ('accent' | 'lead') | null;
    /**
     * The pills listed in this section, in order.
     */
    items?:
      | {
          text: string;
          id?: string | null;
        }[]
      | null;
    /**
     * A CSS gradient for the pill list. Leave empty unless a developer asked you to set it.
     */
    itemsAccent?: string | null;
    /**
     * Optional small print under the list.
     */
    footer?: string | null;
  };
  /**
   * What Google and link previews show. None of this appears on the page itself.
   */
  seo?: {
    /**
     * The browser tab title and the blue headline in Google results. Aim for under 60 characters.
     */
    title?: string | null;
    /**
     * The grey summary under the Google result. Aim for 120–155 characters.
     */
    description?: string | null;
    /**
     * The picture shown when someone shares a link to this page on WhatsApp or LinkedIn. Leave empty to use the site-wide default from Site settings.
     */
    image?: (number | null) | Media;
    /**
     * Leave unticked. Only tick this to keep a page out of Google entirely.
     */
    noindex?: boolean | null;
  };
  updatedAt: string;
  createdAt: string;
  _status?: ('draft' | 'published') | null;
}
/**
 * Photos, screenshots, logos and diagrams used across the site.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "media".
 */
export interface Media {
  id: number;
  /**
   * Describe what is in the image, for people using a screen reader and for when the image fails to load. Say what matters — no need to start with "image of". Leave empty for decorative images that carry no information.
   */
  alt?: string | null;
  /**
   * Optional photographer or source, for your own records.
   */
  credit?: string | null;
  updatedAt: string;
  createdAt: string;
  url?: string | null;
  thumbnailURL?: string | null;
  filename?: string | null;
  mimeType?: string | null;
  filesize?: number | null;
  width?: number | null;
  height?: number | null;
  focalX?: number | null;
  focalY?: number | null;
  sizes?: {
    thumbnail?: {
      url?: string | null;
      width?: number | null;
      height?: number | null;
      mimeType?: string | null;
      filesize?: number | null;
      filename?: string | null;
    };
    card?: {
      url?: string | null;
      width?: number | null;
      height?: number | null;
      mimeType?: string | null;
      filesize?: number | null;
      filename?: string | null;
    };
    wide?: {
      url?: string | null;
      width?: number | null;
      height?: number | null;
      mimeType?: string | null;
      filesize?: number | null;
      filename?: string | null;
    };
    hero?: {
      url?: string | null;
      width?: number | null;
      height?: number | null;
      mimeType?: string | null;
      filesize?: number | null;
      filename?: string | null;
    };
  };
}
/**
 * The privacy policy and terms. These contain commercial terms, so changes here are published deliberately rather than saved automatically.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "legal-pages".
 */
export interface LegalPage {
  id: number;
  /**
   * The page heading, e.g. "Privacy Policy".
   */
  title: string;
  /**
   * The last part of the page address, with no slash — "privacy-policy" makes the page live at sustlabs.com/privacy-policy. Changing this breaks any existing links.
   */
  slug: string;
  /**
   * The opening paragraphs, before the numbered sections.
   */
  intro?: {
    root: {
      type: string;
      children: {
        type: any;
        version: number;
        [k: string]: unknown;
      }[];
      direction: ('ltr' | 'rtl') | null;
      format: 'left' | 'start' | 'center' | 'right' | 'end' | 'justify' | '';
      indent: number;
      version: number;
    };
    [k: string]: unknown;
  } | null;
  /**
   * Each row is one titled section, shown in this order.
   */
  sections?:
    | {
        /**
         * The section heading.
         */
        title: string;
        /**
         * The section text. Lists, links and bold are all available.
         */
        body: {
          root: {
            type: string;
            children: {
              type: any;
              version: number;
              [k: string]: unknown;
            }[];
            direction: ('ltr' | 'rtl') | null;
            format: 'left' | 'start' | 'center' | 'right' | 'end' | 'justify' | '';
            indent: number;
            version: number;
          };
          [k: string]: unknown;
        };
        id?: string | null;
      }[]
    | null;
  /**
   * What Google and link previews show. None of this appears on the page itself.
   */
  seo?: {
    /**
     * The browser tab title and the blue headline in Google results. Aim for under 60 characters.
     */
    title?: string | null;
    /**
     * The grey summary under the Google result. Aim for 120–155 characters.
     */
    description?: string | null;
    /**
     * The picture shown when someone shares a link to this page on WhatsApp or LinkedIn. Leave empty to use the site-wide default from Site settings.
     */
    image?: (number | null) | Media;
    /**
     * Leave unticked. Only tick this to keep a page out of Google entirely.
     */
    noindex?: boolean | null;
  };
  updatedAt: string;
  createdAt: string;
  _status?: ('draft' | 'published') | null;
}
/**
 * Every enquiry submitted through the website. These are also forwarded to the sales Google Sheet — the "Sheet delivery" column shows whether that succeeded.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "leads".
 */
export interface Lead {
  id: number;
  name?: string | null;
  email: string;
  phone?: string | null;
  /**
   * Which page the enquiry came from.
   */
  source: 'smartdb' | 'fms' | 'solution' | 'support' | 'other';
  propertyType?: ('Residential' | 'Commercial' | 'Industrial' | 'Others') | null;
  /**
   * Anything the visitor typed in the message box.
   */
  comment?: string | null;
  /**
   * The enquiry is safely stored here regardless. This only reports whether the copy to the Google Sheet went through.
   */
  forwardStatus?: ('pending' | 'sent' | 'failed') | null;
  forwardedAt?: string | null;
  forwardError?: string | null;
  /**
   * The page the visitor was on when they submitted.
   */
  pagePath?: string | null;
  userAgent?: string | null;
  /**
   * The utm_source tag on the link the visitor arrived through (google, linkedin, a newsletter name). Empty means they came without a tagged link.
   */
  utmSource?: string | null;
  /**
   * The utm_medium tag — cpc, email, social.
   */
  utmMedium?: string | null;
  /**
   * The utm_campaign tag.
   */
  utmCampaign?: string | null;
  updatedAt: string;
  createdAt: string;
}
/**
 * Brochures, PDFs and video offered for download or played on a page.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "documents".
 */
export interface Document {
  id: number;
  /**
   * How this file is listed here in the admin, e.g. "ACMS Brochure 2026".
   */
  title: string;
  /**
   * The name a visitor sees when they save the file, e.g. SustLabs-ACMS-Brochure-2026.pdf. Leave empty to use the uploaded filename.
   */
  downloadFileName?: string | null;
  updatedAt: string;
  createdAt: string;
  url?: string | null;
  thumbnailURL?: string | null;
  filename?: string | null;
  mimeType?: string | null;
  filesize?: number | null;
  width?: number | null;
  height?: number | null;
  focalX?: number | null;
  focalY?: number | null;
}
/**
 * People who can sign in and edit the website.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "users".
 */
export interface User {
  id: number;
  /**
   * Shown instead of the email address around the admin panel.
   */
  name?: string | null;
  updatedAt: string;
  createdAt: string;
  email: string;
  resetPasswordToken?: string | null;
  resetPasswordExpiration?: string | null;
  salt?: string | null;
  hash?: string | null;
  loginAttempts?: number | null;
  lockUntil?: string | null;
  sessions?:
    | {
        id: string;
        createdAt?: string | null;
        expiresAt: string;
      }[]
    | null;
  password?: string | null;
  collection: 'users';
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "payload-kv".
 */
export interface PayloadKv {
  id: number;
  key: string;
  data:
    | {
        [k: string]: unknown;
      }
    | unknown[]
    | string
    | number
    | boolean
    | null;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "payload-locked-documents".
 */
export interface PayloadLockedDocument {
  id: number;
  document?:
    | ({
        relationTo: 'products';
        value: number | Product;
      } | null)
    | ({
        relationTo: 'legal-pages';
        value: number | LegalPage;
      } | null)
    | ({
        relationTo: 'leads';
        value: number | Lead;
      } | null)
    | ({
        relationTo: 'media';
        value: number | Media;
      } | null)
    | ({
        relationTo: 'documents';
        value: number | Document;
      } | null)
    | ({
        relationTo: 'users';
        value: number | User;
      } | null);
  globalSlug?: string | null;
  user: {
    relationTo: 'users';
    value: number | User;
  };
  updatedAt: string;
  createdAt: string;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "payload-preferences".
 */
export interface PayloadPreference {
  id: number;
  user: {
    relationTo: 'users';
    value: number | User;
  };
  key?: string | null;
  value?:
    | {
        [k: string]: unknown;
      }
    | unknown[]
    | string
    | number
    | boolean
    | null;
  updatedAt: string;
  createdAt: string;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "payload-migrations".
 */
export interface PayloadMigration {
  id: number;
  name?: string | null;
  batch?: number | null;
  updatedAt: string;
  createdAt: string;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "products_select".
 */
export interface ProductsSelect<T extends boolean = true> {
  navLabel?: T;
  slug?: T;
  order?: T;
  eyebrow?: T;
  title?: T;
  description?: T;
  introSection?:
    | T
    | {
        titleKicker?: T;
        titleLead?: T;
        titleAccent?: T;
        descriptions?:
          | T
          | {
              text?: T;
              id?: T;
            };
        accent?: T;
      };
  heroSection?:
    | T
    | {
        kicker?: T;
        titleLead?: T;
        titleAccent?: T;
        titleRest?: T;
        description?: T;
        accentColor?: T;
        actions?:
          | T
          | {
              label?: T;
              href?: T;
              variant?: T;
              opensLeadForm?: T;
              analyticsId?: T;
              id?: T;
            };
        image?: T;
        alt?: T;
        imageVariant?: T;
      };
  featureSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        accent?: T;
        accentTarget?: T;
        breakAfterLead?: T;
        cards?:
          | T
          | {
              eyebrow?: T;
              title?: T;
              description?: T;
              id?: T;
            };
      };
  useCaseSection?:
    | T
    | {
        enabled?: T;
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        accent?: T;
        accentTarget?: T;
        breakAfterLead?: T;
        cards?:
          | T
          | {
              number?: T;
              title?: T;
              description?: T;
              id?: T;
            };
      };
  postDetailUseCaseSection?:
    | T
    | {
        enabled?: T;
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        accent?: T;
        accentTarget?: T;
        breakAfterLead?: T;
        cards?:
          | T
          | {
              number?: T;
              title?: T;
              description?: T;
              id?: T;
            };
      };
  ctaSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        accent?: T;
        accentTarget?: T;
        items?:
          | T
          | {
              text?: T;
              id?: T;
            };
        itemsAccent?: T;
        footer?: T;
      };
  seo?:
    | T
    | {
        title?: T;
        description?: T;
        image?: T;
        noindex?: T;
      };
  updatedAt?: T;
  createdAt?: T;
  _status?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "legal-pages_select".
 */
export interface LegalPagesSelect<T extends boolean = true> {
  title?: T;
  slug?: T;
  intro?: T;
  sections?:
    | T
    | {
        title?: T;
        body?: T;
        id?: T;
      };
  seo?:
    | T
    | {
        title?: T;
        description?: T;
        image?: T;
        noindex?: T;
      };
  updatedAt?: T;
  createdAt?: T;
  _status?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "leads_select".
 */
export interface LeadsSelect<T extends boolean = true> {
  name?: T;
  email?: T;
  phone?: T;
  source?: T;
  propertyType?: T;
  comment?: T;
  forwardStatus?: T;
  forwardedAt?: T;
  forwardError?: T;
  pagePath?: T;
  userAgent?: T;
  utmSource?: T;
  utmMedium?: T;
  utmCampaign?: T;
  updatedAt?: T;
  createdAt?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "media_select".
 */
export interface MediaSelect<T extends boolean = true> {
  alt?: T;
  credit?: T;
  updatedAt?: T;
  createdAt?: T;
  url?: T;
  thumbnailURL?: T;
  filename?: T;
  mimeType?: T;
  filesize?: T;
  width?: T;
  height?: T;
  focalX?: T;
  focalY?: T;
  sizes?:
    | T
    | {
        thumbnail?:
          | T
          | {
              url?: T;
              width?: T;
              height?: T;
              mimeType?: T;
              filesize?: T;
              filename?: T;
            };
        card?:
          | T
          | {
              url?: T;
              width?: T;
              height?: T;
              mimeType?: T;
              filesize?: T;
              filename?: T;
            };
        wide?:
          | T
          | {
              url?: T;
              width?: T;
              height?: T;
              mimeType?: T;
              filesize?: T;
              filename?: T;
            };
        hero?:
          | T
          | {
              url?: T;
              width?: T;
              height?: T;
              mimeType?: T;
              filesize?: T;
              filename?: T;
            };
      };
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "documents_select".
 */
export interface DocumentsSelect<T extends boolean = true> {
  title?: T;
  downloadFileName?: T;
  updatedAt?: T;
  createdAt?: T;
  url?: T;
  thumbnailURL?: T;
  filename?: T;
  mimeType?: T;
  filesize?: T;
  width?: T;
  height?: T;
  focalX?: T;
  focalY?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "users_select".
 */
export interface UsersSelect<T extends boolean = true> {
  name?: T;
  updatedAt?: T;
  createdAt?: T;
  email?: T;
  resetPasswordToken?: T;
  resetPasswordExpiration?: T;
  salt?: T;
  hash?: T;
  loginAttempts?: T;
  lockUntil?: T;
  sessions?:
    | T
    | {
        id?: T;
        createdAt?: T;
        expiresAt?: T;
      };
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "payload-kv_select".
 */
export interface PayloadKvSelect<T extends boolean = true> {
  key?: T;
  data?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "payload-locked-documents_select".
 */
export interface PayloadLockedDocumentsSelect<T extends boolean = true> {
  document?: T;
  globalSlug?: T;
  user?: T;
  updatedAt?: T;
  createdAt?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "payload-preferences_select".
 */
export interface PayloadPreferencesSelect<T extends boolean = true> {
  user?: T;
  key?: T;
  value?: T;
  updatedAt?: T;
  createdAt?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "payload-migrations_select".
 */
export interface PayloadMigrationsSelect<T extends boolean = true> {
  name?: T;
  batch?: T;
  updatedAt?: T;
  createdAt?: T;
}
/**
 * Everything on the sustlabs.com homepage.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "home".
 */
export interface Home {
  id: number;
  heroSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The word between the two halves, in the normal colour. Include a trailing space. This used to be fixed in the code.
     */
    titleMiddle?: string | null;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * The buttons shown in this section, left to right.
     */
    actions?:
      | {
          /**
           * The text on the button.
           */
          label: string;
          /**
           * Where it goes. Start with "/" for a page on this site (e.g. /support), "/#" for a section of the homepage, or a full https:// address. Links used in many places — the demo booking link, the store — live under Site settings, so change them there once instead of here.
           */
          href: string;
          /**
           * Primary is the filled button. Use it once per section.
           */
          variant: 'primary' | 'secondary';
          /**
           * Tick this to open the "Explore interest" pop-up form instead of following the link.
           */
          opensLeadForm?: boolean | null;
          /**
           * Used to group clicks on this button in Google Analytics. Set once and then leave it alone — changing it splits the reporting for this button in two.
           */
          analyticsId?: string | null;
          id?: string | null;
        }[]
      | null;
  };
  intelligenceSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * Each card has its own picture. Previously the picture was matched to the card by its title, so renaming a card silently lost its image.
     */
    features?:
      | {
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          /**
           * The picture shown on this card.
           */
          image: number | Media;
          /**
           * Describes the image for screen readers. Leave empty to use the description saved on the image itself in the Images library.
           */
          alt?: string | null;
          id?: string | null;
        }[]
      | null;
  };
  signalLayerSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space. Press Enter to force a line break.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    steps?:
      | {
          /**
           * Shown as the card ordinal. Usually "01." — type it exactly as it should appear, including the full stop.
           */
          number: string;
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
  };
  safetySection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent?: string | null;
    /**
     * Optional last fragment, shown after the highlighted half in the normal colour.
     */
    titleRest?: string | null;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * The diagram of safety detections around Ohm intelligence.
     */
    image: number | Media;
    /**
     * Describes the image for screen readers. Leave empty to use the description saved on the image itself in the Images library.
     */
    alt?: string | null;
  };
  setupSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    points?:
      | {
          /**
           * Shown as the card ordinal. Usually "01." — type it exactly as it should appear, including the full stop.
           */
          number: string;
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
  };
  layersSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    layers?:
      | {
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          /**
           * The screenshot shown on this card.
           */
          image: number | Media;
          /**
           * Describes the image for screen readers. Leave empty to use the description saved on the image itself in the Images library.
           */
          alt?: string | null;
          /**
           * Whether this card uses the light or dark treatment.
           */
          tone: 'light' | 'dark';
          id?: string | null;
        }[]
      | null;
  };
  smartDbSection: {
    eyebrow: string;
    title: string;
    description: string;
    features?:
      | {
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          /**
           * The pastel background for this card. Pick from the list — these are fixed design colours.
           */
          accent: 'mint' | 'lavender' | 'cream' | 'blush' | 'ice';
          id?: string | null;
        }[]
      | null;
  };
  sovereigntySection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    cards?:
      | {
          /**
           * The card heading.
           */
          title: string;
          /**
           * Optional. Leave empty for a title-only card.
           */
          description?: string | null;
          /**
           * The small label at the top of the card.
           */
          eyebrow: string;
          /**
           * Which colour theme this card uses. These are fixed design colours — pick from the list.
           */
          tone: 'teal' | 'violet' | 'amber' | 'coral' | 'blue';
          /**
           * Large cards span more of the grid. Changing this changes the layout.
           */
          size: 'large' | 'regular';
          id?: string | null;
        }[]
      | null;
  };
  enterpriseSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    audiences?:
      | {
          /**
           * Shown as the card ordinal. Usually "01." — type it exactly as it should appear, including the full stop.
           */
          number: string;
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
  };
  impactSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * The proof numbers. These are the most frequently updated content on the site.
     */
    metrics?:
      | {
          /**
           * The figure shown large, e.g. "12k+".
           */
          value: string;
          /**
           * The caption, e.g. "Deployments".
           */
          label: string;
          /**
           * Primary figures are shown larger.
           */
          variant: 'primary' | 'secondary';
          id?: string | null;
        }[]
      | null;
  };
  /**
   * What Google and link previews show. None of this appears on the page itself.
   */
  seo?: {
    /**
     * The browser tab title and the blue headline in Google results. Aim for under 60 characters.
     */
    title?: string | null;
    /**
     * The grey summary under the Google result. Aim for 120–155 characters.
     */
    description?: string | null;
    /**
     * The picture shown when someone shares a link to this page on WhatsApp or LinkedIn. Leave empty to use the site-wide default from Site settings.
     */
    image?: (number | null) | Media;
    /**
     * Leave unticked. Only tick this to keep a page out of Google entirely.
     */
    noindex?: boolean | null;
  };
  _status?: ('draft' | 'published') | null;
  updatedAt?: string | null;
  createdAt?: string | null;
}
/**
 * Everything on sustlabs.com/smart-db. The sections appear on the page in the order of the tabs below. To move or remove a section, ask a developer.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "smart-db".
 */
export interface SmartDb {
  id: number;
  heroSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * The buttons shown in this section, left to right.
     */
    actions?:
      | {
          /**
           * The text on the button.
           */
          label: string;
          /**
           * Where it goes. Start with "/" for a page on this site (e.g. /support), "/#" for a section of the homepage, or a full https:// address. Links used in many places — the demo booking link, the store — live under Site settings, so change them there once instead of here.
           */
          href: string;
          /**
           * Primary is the filled button. Use it once per section.
           */
          variant: 'primary' | 'secondary';
          /**
           * Tick this to open the "Explore interest" pop-up form instead of following the link.
           */
          opensLeadForm?: boolean | null;
          /**
           * Used to group clicks on this button in Google Analytics. Set once and then leave it alone — changing it splits the reporting for this button in two.
           */
          analyticsId?: string | null;
          id?: string | null;
        }[]
      | null;
  };
  /**
   * The "Intelligence Layer" and "Conventional Layer" callouts are drawn into the image itself, so there is no text to edit for them here.
   */
  layersSection: {
    title: string;
    /**
     * The exploded view of the Smart DB enclosure.
     */
    image: number | Media;
    /**
     * Describes the image for screen readers. Leave empty to use the description saved on the image itself in the Images library.
     */
    alt?: string | null;
  };
  knowsSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * Each row becomes its own paragraph, in this order.
     */
    descriptions?:
      | {
          text: string;
          id?: string | null;
        }[]
      | null;
    /**
     * Shown as a row of figures under the paragraphs.
     */
    stats?:
      | {
          /**
           * The caption, e.g. "Circuit-level sensing".
           */
          label: string;
          /**
           * The word or figure shown large, e.g. "Live".
           */
          value: string;
          id?: string | null;
        }[]
      | null;
    /**
     * The underlined link with an arrow at the end of this section. Clear the label to hide it.
     */
    action?: {
      /**
       * The link text. Leave empty to hide the link entirely.
       */
      label?: string | null;
      /**
       * Where it goes. Start with "/" for a page on this site (e.g. /support), "/#" for a section of the homepage, or a full https:// address. Links used in many places — the demo booking link, the store — live under Site settings, so change them there once instead of here.
       */
      href?: string | null;
      /**
       * Pick a file here instead of typing a link, when the link should download something. If set, this wins over the Link field above.
       */
      document?: (number | null) | Document;
      /**
       * Tick this to open the "Explore interest" pop-up form instead of following the link.
       */
      opensLeadForm?: boolean | null;
    };
  };
  unchangedSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * The shortcomings of a conventional distribution board.
     */
    cards?:
      | {
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
  };
  loopSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * The continuous loop, in order.
     */
    steps?:
      | {
          /**
           * Shown as the card ordinal. Usually "01." — type it exactly as it should appear, including the full stop.
           */
          number: string;
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
    /**
     * The underlined link with an arrow at the end of this section. Clear the label to hide it.
     */
    action?: {
      /**
       * The link text. Leave empty to hide the link entirely.
       */
      label?: string | null;
      /**
       * Where it goes. Start with "/" for a page on this site (e.g. /support), "/#" for a section of the homepage, or a full https:// address. Links used in many places — the demo booking link, the store — live under Site settings, so change them there once instead of here.
       */
      href?: string | null;
      /**
       * Pick a file here instead of typing a link, when the link should download something. If set, this wins over the Link field above.
       */
      document?: (number | null) | Document;
      /**
       * Tick this to open the "Explore interest" pop-up form instead of following the link.
       */
      opensLeadForm?: boolean | null;
    };
  };
  capabilitiesSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    items?:
      | {
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
  };
  pocketSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * The first card in the row. Its eyebrow is reused as the caption under the card, so it is only typed once.
     */
    highlight: {
      eyebrow: string;
      title: string;
      description: string;
    };
    /**
     * The phone screenshots in the scrolling row, left to right.
     */
    screens?:
      | {
          /**
           * The phone screenshot. Use the same shape and size for all of them, or the row will look uneven.
           */
          image: number | Media;
          /**
           * Describes the image for screen readers. Leave empty to use the description saved on the image itself in the Images library.
           */
          alt?: string | null;
          /**
           * The short label under the phone, e.g. "Home overview".
           */
          caption: string;
          id?: string | null;
        }[]
      | null;
  };
  platformSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    tiles?:
      | {
          /**
           * e.g. "Web App / Dashboard".
           */
          name: string;
          /**
           * The short line under the name describing where it runs, e.g. "Mobile · On the go".
           */
          kicker: string;
          description: string;
          /**
           * The picture shown inside this tile.
           */
          image: number | Media;
          /**
           * Describes the image for screen readers. Leave empty to use the description saved on the image itself in the Images library.
           */
          alt?: string | null;
          /**
           * Controls the shape of the frame drawn around the picture, not the picture itself. Changing this changes the tile layout.
           */
          variant: 'app' | 'dashboard' | 'clock';
          id?: string | null;
        }[]
      | null;
  };
  comparisonSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The header row, left to right. Every row below must have exactly this many cells, in the same order.
     */
    columns?:
      | {
          text: string;
          id?: string | null;
        }[]
      | null;
    /**
     * One row per capability. The first cell becomes the row heading.
     */
    rows?:
      | {
          /**
           * In the same order as the columns above.
           */
          cells?:
            | {
                text: string;
                id?: string | null;
              }[]
            | null;
          id?: string | null;
        }[]
      | null;
    /**
     * The underlined link with an arrow at the end of this section. Clear the label to hide it.
     */
    action?: {
      /**
       * The link text. Leave empty to hide the link entirely.
       */
      label?: string | null;
      /**
       * Where it goes. Start with "/" for a page on this site (e.g. /support), "/#" for a section of the homepage, or a full https:// address. Links used in many places — the demo booking link, the store — live under Site settings, so change them there once instead of here.
       */
      href?: string | null;
      /**
       * Pick a file here instead of typing a link, when the link should download something. If set, this wins over the Link field above.
       */
      document?: (number | null) | Document;
      /**
       * Tick this to open the "Explore interest" pop-up form instead of following the link.
       */
      opensLeadForm?: boolean | null;
    };
  };
  segmentsSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    segments?:
      | {
          /**
           * e.g. "New apartments".
           */
          name: string;
          description: string;
          /**
           * A photograph of this kind of building or site.
           */
          image: number | Media;
          /**
           * Describes the image for screen readers. Leave empty to use the description saved on the image itself in the Images library.
           */
          alt?: string | null;
          id?: string | null;
        }[]
      | null;
  };
  fireSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * The short pills under the paragraph, e.g. "Certified electrical installation".
     */
    tags?:
      | {
          text: string;
          id?: string | null;
        }[]
      | null;
  };
  specsSection?: {
    /**
     * Turns the specifications table on or off on the live site. The wording is kept either way, so you can switch it back on later without retyping anything.
     */
    enabled?: boolean | null;
    title?: string | null;
    /**
     * One row per specification.
     */
    specs?:
      | {
          /**
           * What is being specified, e.g. "Rated voltage".
           */
          label: string;
          /**
           * The value, e.g. "240V AC, 50Hz".
           */
          value: string;
          id?: string | null;
        }[]
      | null;
    /**
     * Small print under the table.
     */
    note?: string | null;
  };
  /**
   * What Google and link previews show. None of this appears on the page itself.
   */
  seo?: {
    /**
     * The browser tab title and the blue headline in Google results. Aim for under 60 characters.
     */
    title?: string | null;
    /**
     * The grey summary under the Google result. Aim for 120–155 characters.
     */
    description?: string | null;
    /**
     * The picture shown when someone shares a link to this page on WhatsApp or LinkedIn. Leave empty to use the site-wide default from Site settings.
     */
    image?: (number | null) | Media;
    /**
     * Leave unticked. Only tick this to keep a page out of Google entirely.
     */
    noindex?: boolean | null;
  };
  _status?: ('draft' | 'published') | null;
  updatedAt?: string | null;
  createdAt?: string | null;
}
/**
 * Everything on sustlabs.com/fms — the fire monitoring system page.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "fms".
 */
export interface Fm {
  id: number;
  heroSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half. Press Enter to force a line break.
     */
    titleAccent: string;
    /**
     * Optional small badge above the heading.
     */
    badge?: string | null;
    /**
     * The row of figures under the heading.
     */
    stats?:
      | {
          /**
           * The caption under it, e.g. "Parameters monitored live".
           */
          label: string;
          /**
           * The figure shown large, e.g. "20+" or "24/7".
           */
          value: string;
          id?: string | null;
        }[]
      | null;
    /**
     * The buttons shown in this section, left to right.
     */
    actions?:
      | {
          /**
           * The text on the button.
           */
          label: string;
          /**
           * Where it goes. Start with "/" for a page on this site (e.g. /support), "/#" for a section of the homepage, or a full https:// address. Links used in many places — the demo booking link, the store — live under Site settings, so change them there once instead of here.
           */
          href: string;
          /**
           * Primary is the filled button. Use it once per section.
           */
          variant: 'primary' | 'secondary';
          /**
           * Tick this to open the "Explore interest" pop-up form instead of following the link.
           */
          opensLeadForm?: boolean | null;
          /**
           * Used to group clicks on this button in Google Analytics. Set once and then leave it alone — changing it splits the reporting for this button in two.
           */
          analyticsId?: string | null;
          id?: string | null;
        }[]
      | null;
  };
  problemSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half. Press Enter to force a line break.
     */
    titleAccent?: string | null;
    /**
     * Optional paragraph under the heading. Leave empty to show none.
     */
    description?: string | null;
    /**
     * The dashboard screenshot shown beside this section.
     */
    image: number | Media;
    /**
     * Describes the image for screen readers. Leave empty to use the description saved on the image itself in the Images library.
     */
    alt?: string | null;
    /**
     * Optional. Leave empty for no caption.
     */
    mediaCaption?: string | null;
    /**
     * Clear the label to hide the download link.
     */
    downloadAction?: {
      /**
       * Link text, e.g. "Download the brochure".
       */
      label?: string | null;
      /**
       * The file offered for download.
       */
      document?: (number | null) | Document;
    };
  };
  audienceSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent?: string | null;
    /**
     * Optional paragraph under the heading. Leave empty to show none.
     */
    description?: string | null;
    /**
     * The image showing the kinds of sites FMS is deployed across.
     */
    image: number | Media;
    /**
     * Describes the image for screen readers. Leave empty to use the description saved on the image itself in the Images library.
     */
    alt?: string | null;
    /**
     * Optional. Leave empty for no caption.
     */
    mediaCaption?: string | null;
  };
  gapSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent?: string | null;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * Optional word in the heading given extra emphasis.
     */
    titleHighlight?: string | null;
    /**
     * Exactly two columns: the periodic-inspection side and the continuous-monitoring side.
     */
    columns?:
      | {
          eyebrow: string;
          title: string;
          /**
           * Controls whether this column shows ticks or crosses beside each line. Changing it changes the meaning of the column.
           */
          tone: 'inspection' | 'readiness';
          /**
           * One line per point, in order.
           */
          items?:
            | {
                text: string;
                id?: string | null;
              }[]
            | null;
          id?: string | null;
        }[]
      | null;
  };
  chainSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half. Press Enter to force a line break.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * The looping animation played behind this section. Muted and autoplaying, so it must not rely on sound.
     */
    video: number | Document;
    /**
     * Describes the animation for screen readers.
     */
    mediaAlt: string;
    items?:
      | {
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
  };
  stepsSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent?: string | null;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    cards?:
      | {
          /**
           * Shown as the card ordinal. Usually "01." — type it exactly as it should appear, including the full stop.
           */
          number: string;
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
  };
  coverageSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent?: string | null;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * Each group is one card listing the parameters it covers.
     */
    groups?:
      | {
          /**
           * The group name, e.g. "Pumps".
           */
          title: string;
          /**
           * One per line, in the order they should appear.
           */
          parameters?:
            | {
                text: string;
                id?: string | null;
              }[]
            | null;
          id?: string | null;
        }[]
      | null;
  };
  visibilitySection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent?: string | null;
    /**
     * Optional paragraph under the heading. Leave empty to show none.
     */
    description?: string | null;
    /**
     * The console screenshot shown beside this section.
     */
    image: number | Media;
    /**
     * Describes the image for screen readers. Leave empty to use the description saved on the image itself in the Images library.
     */
    alt?: string | null;
    /**
     * Optional. Leave empty for no caption.
     */
    mediaCaption?: string | null;
  };
  alertsSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent?: string | null;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * Realistic examples of the messages the system sends. These are illustrations shown on the page, not live data.
     */
    cards?:
      | {
          /**
           * How it is delivered, e.g. "SMS" or "EMAIL".
           */
          meta: string;
          /**
           * Sets the colour of the alert card.
           */
          tone: 'critical' | 'advisory' | 'summary';
          text: string;
          id?: string | null;
        }[]
      | null;
  };
  assuranceSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent?: string | null;
    /**
     * Optional paragraph under the heading. Leave empty to show none.
     */
    description?: string | null;
    items?:
      | {
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
  };
  faqSection: {
    /**
     * Turns the FAQ on or off on the live site. The wording is kept either way, so you can switch it back on later without retyping anything.
     */
    enabled?: boolean | null;
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent?: string | null;
    /**
     * Optional paragraph under the heading. Leave empty to show none.
     */
    description?: string | null;
    items?:
      | {
          title: string;
          answer: string;
          id?: string | null;
        }[]
      | null;
    /**
     * Small print under the questions.
     */
    note?: string | null;
    action?: {
      label?: string | null;
      href?: string | null;
    };
  };
  /**
   * What Google and link previews show. None of this appears on the page itself.
   */
  seo?: {
    /**
     * The browser tab title and the blue headline in Google results. Aim for under 60 characters.
     */
    title?: string | null;
    /**
     * The grey summary under the Google result. Aim for 120–155 characters.
     */
    description?: string | null;
    /**
     * The picture shown when someone shares a link to this page on WhatsApp or LinkedIn. Leave empty to use the site-wide default from Site settings.
     */
    image?: (number | null) | Media;
    /**
     * Leave unticked. Only tick this to keep a page out of Google entirely.
     */
    noindex?: boolean | null;
  };
  _status?: ('draft' | 'published') | null;
  updatedAt?: string | null;
  createdAt?: string | null;
}
/**
 * Everything on sustlabs.com/ohm-os.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "ohm-os".
 */
export interface OhmO {
  id: number;
  heroSection: {
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * Optional last fragment, shown after the highlighted half in the normal colour.
     */
    titleRest?: string | null;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * The buttons shown in this section, left to right.
     */
    actions?:
      | {
          /**
           * The text on the button.
           */
          label: string;
          /**
           * Where it goes. Start with "/" for a page on this site (e.g. /support), "/#" for a section of the homepage, or a full https:// address. Links used in many places — the demo booking link, the store — live under Site settings, so change them there once instead of here.
           */
          href: string;
          /**
           * Primary is the filled button. Use it once per section.
           */
          variant: 'primary' | 'secondary';
          /**
           * Tick this to open the "Explore interest" pop-up form instead of following the link.
           */
          opensLeadForm?: boolean | null;
          /**
           * Used to group clicks on this button in Google Analytics. Set once and then leave it alone — changing it splits the reporting for this button in two.
           */
          analyticsId?: string | null;
          id?: string | null;
        }[]
      | null;
  };
  /**
   * The "Our Ecosystem" band, with the diagram and the list of applications.
   */
  applicationsSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent?: string | null;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * The Ohm OS ecosystem diagram.
     */
    image: number | Media;
    /**
     * Describes the image for screen readers. Leave empty to use the description saved on the image itself in the Images library.
     */
    alt?: string | null;
    items?:
      | {
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
  };
  layerSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * The highlight colour for this heading.
     */
    accent:
      | 'var(--terra500)'
      | 'var(--teal500)'
      | 'var(--success500)'
      | 'var(--black300)'
      | 'var(--black400)'
      | 'var(--black500)'
      | 'var(--white)'
      | '#145599'
      | '#4CAF53';
    /**
     * One card per product tier. The small label is the product code, e.g. "o3".
     */
    cards?:
      | {
          /**
           * The small label above the card heading, e.g. "Live Usage".
           */
          eyebrow: string;
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
  };
  licenseeSection: {
    title: string;
    description: string;
    /**
     * Shown left to right. Drag to reorder.
     */
    logos?:
      | {
          /**
           * The partner name, also used as the image alt text.
           */
          label: string;
          image: number | Media;
          /**
           * Each partner logo has its own size and spacing in the design. Pick the preset that matches this logo. Adding a brand that is not on this list needs a developer.
           */
          slug: 'panasonic' | 'schneider' | 'inepro' | 'legrand';
          id?: string | null;
        }[]
      | null;
  };
  /**
   * What Google and link previews show. None of this appears on the page itself.
   */
  seo?: {
    /**
     * The browser tab title and the blue headline in Google results. Aim for under 60 characters.
     */
    title?: string | null;
    /**
     * The grey summary under the Google result. Aim for 120–155 characters.
     */
    description?: string | null;
    /**
     * The picture shown when someone shares a link to this page on WhatsApp or LinkedIn. Leave empty to use the site-wide default from Site settings.
     */
    image?: (number | null) | Media;
    /**
     * Leave unticked. Only tick this to keep a page out of Google entirely.
     */
    noindex?: boolean | null;
  };
  _status?: ('draft' | 'published') | null;
  updatedAt?: string | null;
  createdAt?: string | null;
}
/**
 * Everything on sustlabs.com/add-ons/ora.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "ora".
 */
export interface Ora {
  id: number;
  heroSection: {
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * The large photograph of Ora on a wall.
     */
    image: number | Media;
    /**
     * Describes the image for screen readers. Leave empty to use the description saved on the image itself in the Images library.
     */
    alt?: string | null;
    /**
     * The small panel laid over the photograph. Previously fixed in code.
     */
    callout: {
      title: string;
      description: string;
    };
    action: {
      label: string;
      /**
       * Start with "/" for a page on this site, or a full https:// address.
       */
      href: string;
      /**
       * Primary is the filled button. Use it once per section.
       */
      variant: 'primary' | 'secondary';
    };
  };
  enablesSection: {
    /**
     * The image showing Ora’s display states. This is the only part of this section currently shown on the site.
     */
    image: number | Media;
    /**
     * Describes the image for screen readers. Leave empty to use the description saved on the image itself in the Images library.
     */
    alt?: string | null;
    /**
     * Turns the heading and cards above the image on or off on the live site. The wording is kept either way, so you can switch it back on later without retyping anything.
     */
    enabled?: boolean | null;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    cards?:
      | {
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
  };
  /**
   * What Google and link previews show. None of this appears on the page itself.
   */
  seo?: {
    /**
     * The browser tab title and the blue headline in Google results. Aim for under 60 characters.
     */
    title?: string | null;
    /**
     * The grey summary under the Google result. Aim for 120–155 characters.
     */
    description?: string | null;
    /**
     * The picture shown when someone shares a link to this page on WhatsApp or LinkedIn. Leave empty to use the site-wide default from Site settings.
     */
    image?: (number | null) | Media;
    /**
     * Leave unticked. Only tick this to keep a page out of Google entirely.
     */
    noindex?: boolean | null;
  };
  _status?: ('draft' | 'published') | null;
  updatedAt?: string | null;
  createdAt?: string | null;
}
/**
 * Everything on sustlabs.com/solutions.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "solutions".
 */
export interface Solution {
  id: number;
  heroSection: {
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * Optional last fragment, shown after the highlighted half in the normal colour.
     */
    titleRest?: string | null;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * The line under the buttons.
     */
    note: string;
    /**
     * The buttons shown in this section, left to right.
     */
    actions?:
      | {
          /**
           * The text on the button.
           */
          label: string;
          /**
           * Where it goes. Start with "/" for a page on this site (e.g. /support), "/#" for a section of the homepage, or a full https:// address. Links used in many places — the demo booking link, the store — live under Site settings, so change them there once instead of here.
           */
          href: string;
          /**
           * Primary is the filled button. Use it once per section.
           */
          variant: 'primary' | 'secondary';
          /**
           * Tick this to open the "Explore interest" pop-up form instead of following the link.
           */
          opensLeadForm?: boolean | null;
          /**
           * Used to group clicks on this button in Google Analytics. Set once and then leave it alone — changing it splits the reporting for this button in two.
           */
          analyticsId?: string | null;
          id?: string | null;
        }[]
      | null;
  };
  /**
   * One panel per vertical, in order. They are numbered automatically, so adding or reordering panels renumbers them for you.
   */
  panels?:
    | {
        /**
         * The small label above the panel heading.
         */
        eyebrow?: string | null;
        /**
         * Optional word before the heading.
         */
        titlePrefix?: string | null;
        /**
         * Renders in the normal colour. End with a space if the two halves sit on one line.
         */
        title: string;
        titleAccent?: string | null;
        description?: string | null;
        /**
         * The bullet list shown in this panel.
         */
        capabilities?:
          | {
              text: string;
              id?: string | null;
            }[]
          | null;
        id?: string | null;
      }[]
    | null;
  /**
   * The final band. It is numbered after the panels above, automatically.
   */
  partnerSection: {
    /**
     * The small line above the heading, e.g. "THE PRODUCT". Type it in the case you want to see — some sections show it in capitals, others exactly as typed.
     */
    eyebrow: string;
    /**
     * The first half of the heading. Shown in the normal text colour. If both halves need to sit on the same line, end this field with a space.
     */
    titleLead: string;
    /**
     * The second half of the heading. Shown in the highlight colour. Leave empty if this heading has no highlighted half.
     */
    titleAccent: string;
    /**
     * The paragraph shown under the heading.
     */
    description: string;
    /**
     * One of the fixed design colours. Pick from the list — do not type a colour code.
     */
    accent:
      | 'var(--terra500)'
      | 'var(--teal500)'
      | 'var(--success500)'
      | 'var(--black300)'
      | 'var(--black400)'
      | 'var(--black500)'
      | 'var(--white)'
      | '#145599'
      | '#4CAF53';
    /**
     * Which half of the heading gets the highlight colour above.
     */
    accentTarget?: ('accent' | 'lead') | null;
    /**
     * On by default. Turn it off to keep the whole heading on one line. This changes the layout, not the wording.
     */
    breakAfterLead?: boolean | null;
    items?:
      | {
          /**
           * The card heading.
           */
          title: string;
          /**
           * One or two sentences under the card heading.
           */
          description: string;
          id?: string | null;
        }[]
      | null;
  };
  /**
   * What Google and link previews show. None of this appears on the page itself.
   */
  seo?: {
    /**
     * The browser tab title and the blue headline in Google results. Aim for under 60 characters.
     */
    title?: string | null;
    /**
     * The grey summary under the Google result. Aim for 120–155 characters.
     */
    description?: string | null;
    /**
     * The picture shown when someone shares a link to this page on WhatsApp or LinkedIn. Leave empty to use the site-wide default from Site settings.
     */
    image?: (number | null) | Media;
    /**
     * Leave unticked. Only tick this to keep a page out of Google entirely.
     */
    noindex?: boolean | null;
  };
  _status?: ('draft' | 'published') | null;
  updatedAt?: string | null;
  createdAt?: string | null;
}
/**
 * Everything on sustlabs.com/support.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "support".
 */
export interface Support {
  id: number;
  heroSection: {
    title: string;
    description: string;
    action: {
      label: string;
      /**
       * Shared links such as the demo booking URL live under Site settings.
       */
      href: string;
      /**
       * Primary is the filled button. Use it once per section.
       */
      variant: 'primary' | 'secondary';
    };
  };
  /**
   * The scannable cards under the heading. These used to be fixed in code, so changing a support number meant a developer and a deploy.
   */
  qrCards?:
    | {
        /**
         * The card heading, e.g. "Scan to Chat".
         */
        label: string;
        description: string;
        /**
         * The scannable code itself. Replace this to point at a different number or WhatsApp account.
         */
        qrImage: number | Media;
        /**
         * The small badge shown on the card.
         */
        icon: number | Media;
        id?: string | null;
      }[]
    | null;
  contactSection?: {
    /**
     * Turns the contact form on or off on the live site. The wording is kept either way, so you can switch it back on later without retyping anything.
     */
    enabled?: boolean | null;
    title?: string | null;
  };
  /**
   * What Google and link previews show. None of this appears on the page itself.
   */
  seo?: {
    /**
     * The browser tab title and the blue headline in Google results. Aim for under 60 characters.
     */
    title?: string | null;
    /**
     * The grey summary under the Google result. Aim for 120–155 characters.
     */
    description?: string | null;
    /**
     * The picture shown when someone shares a link to this page on WhatsApp or LinkedIn. Leave empty to use the site-wide default from Site settings.
     */
    image?: (number | null) | Media;
    /**
     * Leave unticked. Only tick this to keep a page out of Google entirely.
     */
    noindex?: boolean | null;
  };
  _status?: ('draft' | 'published') | null;
  updatedAt?: string | null;
  createdAt?: string | null;
}
/**
 * The navigation bar at the top of every page, and the footer at the bottom. Drag rows to reorder them. Removing a row removes the link from the site.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "navigation".
 */
export interface Navigation {
  id: number;
  /**
   * Shown left to right across the top of every page. Add rows under "Dropdown items" to turn an entry into a dropdown menu.
   */
  header?:
    | {
        /**
         * The words a visitor sees.
         */
        label: string;
        /**
         * Leave empty if this entry only opens a dropdown. Start with "/" for a page on this site (e.g. /smart-db), "/#" for a section of the homepage (e.g. /#products), or a full https:// address for another website.
         */
        url?: string | null;
        /**
         * Optional. Highlights this entry whenever the visitor is anywhere under this path, e.g. /products. Leave empty to highlight only on an exact match.
         */
        activePathPrefix?: string | null;
        newTab?: boolean | null;
        /**
         * Leave empty for a plain link. Add rows to make this a dropdown — this is where the Products and Add-ons menus are listed, so a new product can be added to the menu here without a developer.
         */
        children?:
          | {
              /**
               * The words a visitor sees.
               */
              label: string;
              /**
               * Start with "/" for a page on this site (e.g. /smart-db), "/#" for a section of the homepage (e.g. /#products), or a full https:// address for another website.
               */
              url: string;
              /**
               * Tick this for links to other websites.
               */
              newTab?: boolean | null;
              id?: string | null;
            }[]
          | null;
        id?: string | null;
      }[]
    | null;
  footer: {
    /**
     * The line under the logo, e.g. "Rewiring the Future of Homes".
     */
    tagline: string;
    /**
     * The columns of links, left to right.
     */
    columns?:
      | {
          /**
           * The column heading, e.g. "Explore".
           */
          title: string;
          links?:
            | {
                /**
                 * The words a visitor sees.
                 */
                label: string;
                /**
                 * Start with "/" for a page on this site (e.g. /smart-db), "/#" for a section of the homepage (e.g. /#products), or a full https:// address for another website.
                 */
                url: string;
                /**
                 * Tick this for links to other websites.
                 */
                newTab?: boolean | null;
                id?: string | null;
              }[]
            | null;
          id?: string | null;
        }[]
      | null;
    /**
     * The small links along the bottom, beside the copyright line.
     */
    legalLinks?:
      | {
          /**
           * The words a visitor sees.
           */
          label: string;
          /**
           * Start with "/" for a page on this site (e.g. /smart-db), "/#" for a section of the homepage (e.g. /#products), or a full https:// address for another website.
           */
          url: string;
          /**
           * Tick this for links to other websites.
           */
          newTab?: boolean | null;
          id?: string | null;
        }[]
      | null;
    /**
     * The copyright line at the very bottom, e.g. "Copyright © 2026 Sustlabs. All rights reserved."
     */
    copyright: string;
  };
  updatedAt?: string | null;
  createdAt?: string | null;
}
/**
 * Links and details used in many places at once. Change them here and they update everywhere on the site.
 *
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "settings".
 */
export interface Setting {
  id: number;
  /**
   * The Calendly booking link. Used by the "Book a Demo" button on the homepage, Ohm OS, Smart DB, FMS, Solutions and the footer.
   */
  demoBookingUrl: string;
  /**
   * The MirAIe Lifestyle storefront, where o3 and o4 are sold.
   */
  storeUrl?: string | null;
  /**
   * The Gen3 setup guide, currently hosted on Google Drive.
   */
  setupGuideUrl?: string | null;
  /**
   * The short product overview video on YouTube.
   */
  overviewVideoUrl?: string | null;
  /**
   * Where sales enquiries should be sent.
   */
  salesEmail?: string | null;
  /**
   * Shown in the privacy policy and terms.
   */
  supportEmail?: string | null;
  /**
   * The single line shown at the bottom right of every page, e.g. "Aspire Research Park, IIT Bombay, Mumbai (India)".
   */
  officeAddress: string;
  /**
   * The full registered company name, used in the privacy policy and terms.
   */
  legalEntityName?: string | null;
  /**
   * The logo shown in the header and footer.
   */
  logo?: (number | null) | Media;
  /**
   * The fallback picture shown when someone shares a link to this site on WhatsApp or LinkedIn. Individual pages can override it.
   */
  defaultShareImage?: (number | null) | Media;
  /**
   * Used for any page that has not set its own search title.
   */
  defaultSeoTitle: string;
  /**
   * Used for any page without its own search summary.
   */
  defaultSeoDescription: string;
  /**
   * The public address of the site, with no trailing slash. Used to build the canonical URLs and the sitemap. Only change this if the domain changes.
   */
  canonicalOrigin: string;
  updatedAt?: string | null;
  createdAt?: string | null;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "home_select".
 */
export interface HomeSelect<T extends boolean = true> {
  heroSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleMiddle?: T;
        titleAccent?: T;
        description?: T;
        actions?:
          | T
          | {
              label?: T;
              href?: T;
              variant?: T;
              opensLeadForm?: T;
              analyticsId?: T;
              id?: T;
            };
      };
  intelligenceSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        features?:
          | T
          | {
              title?: T;
              description?: T;
              image?: T;
              alt?: T;
              id?: T;
            };
      };
  signalLayerSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        steps?:
          | T
          | {
              number?: T;
              title?: T;
              description?: T;
              id?: T;
            };
      };
  safetySection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        titleRest?: T;
        description?: T;
        image?: T;
        alt?: T;
      };
  setupSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        points?:
          | T
          | {
              number?: T;
              title?: T;
              description?: T;
              id?: T;
            };
      };
  layersSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        layers?:
          | T
          | {
              title?: T;
              description?: T;
              image?: T;
              alt?: T;
              tone?: T;
              id?: T;
            };
      };
  smartDbSection?:
    | T
    | {
        eyebrow?: T;
        title?: T;
        description?: T;
        features?:
          | T
          | {
              title?: T;
              description?: T;
              accent?: T;
              id?: T;
            };
      };
  sovereigntySection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        cards?:
          | T
          | {
              title?: T;
              description?: T;
              eyebrow?: T;
              tone?: T;
              size?: T;
              id?: T;
            };
      };
  enterpriseSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        audiences?:
          | T
          | {
              number?: T;
              title?: T;
              description?: T;
              id?: T;
            };
      };
  impactSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        metrics?:
          | T
          | {
              value?: T;
              label?: T;
              variant?: T;
              id?: T;
            };
      };
  seo?:
    | T
    | {
        title?: T;
        description?: T;
        image?: T;
        noindex?: T;
      };
  _status?: T;
  updatedAt?: T;
  createdAt?: T;
  globalType?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "smart-db_select".
 */
export interface SmartDbSelect<T extends boolean = true> {
  heroSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        actions?:
          | T
          | {
              label?: T;
              href?: T;
              variant?: T;
              opensLeadForm?: T;
              analyticsId?: T;
              id?: T;
            };
      };
  layersSection?:
    | T
    | {
        title?: T;
        image?: T;
        alt?: T;
      };
  knowsSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        descriptions?:
          | T
          | {
              text?: T;
              id?: T;
            };
        stats?:
          | T
          | {
              label?: T;
              value?: T;
              id?: T;
            };
        action?:
          | T
          | {
              label?: T;
              href?: T;
              document?: T;
              opensLeadForm?: T;
            };
      };
  unchangedSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        cards?:
          | T
          | {
              title?: T;
              description?: T;
              id?: T;
            };
      };
  loopSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        steps?:
          | T
          | {
              number?: T;
              title?: T;
              description?: T;
              id?: T;
            };
        action?:
          | T
          | {
              label?: T;
              href?: T;
              document?: T;
              opensLeadForm?: T;
            };
      };
  capabilitiesSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        items?:
          | T
          | {
              title?: T;
              description?: T;
              id?: T;
            };
      };
  pocketSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        highlight?:
          | T
          | {
              eyebrow?: T;
              title?: T;
              description?: T;
            };
        screens?:
          | T
          | {
              image?: T;
              alt?: T;
              caption?: T;
              id?: T;
            };
      };
  platformSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        tiles?:
          | T
          | {
              name?: T;
              kicker?: T;
              description?: T;
              image?: T;
              alt?: T;
              variant?: T;
              id?: T;
            };
      };
  comparisonSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        columns?:
          | T
          | {
              text?: T;
              id?: T;
            };
        rows?:
          | T
          | {
              cells?:
                | T
                | {
                    text?: T;
                    id?: T;
                  };
              id?: T;
            };
        action?:
          | T
          | {
              label?: T;
              href?: T;
              document?: T;
              opensLeadForm?: T;
            };
      };
  segmentsSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        segments?:
          | T
          | {
              name?: T;
              description?: T;
              image?: T;
              alt?: T;
              id?: T;
            };
      };
  fireSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        tags?:
          | T
          | {
              text?: T;
              id?: T;
            };
      };
  specsSection?:
    | T
    | {
        enabled?: T;
        title?: T;
        specs?:
          | T
          | {
              label?: T;
              value?: T;
              id?: T;
            };
        note?: T;
      };
  seo?:
    | T
    | {
        title?: T;
        description?: T;
        image?: T;
        noindex?: T;
      };
  _status?: T;
  updatedAt?: T;
  createdAt?: T;
  globalType?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "fms_select".
 */
export interface FmsSelect<T extends boolean = true> {
  heroSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        badge?: T;
        stats?:
          | T
          | {
              label?: T;
              value?: T;
              id?: T;
            };
        actions?:
          | T
          | {
              label?: T;
              href?: T;
              variant?: T;
              opensLeadForm?: T;
              analyticsId?: T;
              id?: T;
            };
      };
  problemSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        image?: T;
        alt?: T;
        mediaCaption?: T;
        downloadAction?:
          | T
          | {
              label?: T;
              document?: T;
            };
      };
  audienceSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        image?: T;
        alt?: T;
        mediaCaption?: T;
      };
  gapSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        titleHighlight?: T;
        columns?:
          | T
          | {
              eyebrow?: T;
              title?: T;
              tone?: T;
              items?:
                | T
                | {
                    text?: T;
                    id?: T;
                  };
              id?: T;
            };
      };
  chainSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        video?: T;
        mediaAlt?: T;
        items?:
          | T
          | {
              title?: T;
              description?: T;
              id?: T;
            };
      };
  stepsSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        cards?:
          | T
          | {
              number?: T;
              title?: T;
              description?: T;
              id?: T;
            };
      };
  coverageSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        groups?:
          | T
          | {
              title?: T;
              parameters?:
                | T
                | {
                    text?: T;
                    id?: T;
                  };
              id?: T;
            };
      };
  visibilitySection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        image?: T;
        alt?: T;
        mediaCaption?: T;
      };
  alertsSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        cards?:
          | T
          | {
              meta?: T;
              tone?: T;
              text?: T;
              id?: T;
            };
      };
  assuranceSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        items?:
          | T
          | {
              title?: T;
              description?: T;
              id?: T;
            };
      };
  faqSection?:
    | T
    | {
        enabled?: T;
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        items?:
          | T
          | {
              title?: T;
              answer?: T;
              id?: T;
            };
        note?: T;
        action?:
          | T
          | {
              label?: T;
              href?: T;
            };
      };
  seo?:
    | T
    | {
        title?: T;
        description?: T;
        image?: T;
        noindex?: T;
      };
  _status?: T;
  updatedAt?: T;
  createdAt?: T;
  globalType?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "ohm-os_select".
 */
export interface OhmOsSelect<T extends boolean = true> {
  heroSection?:
    | T
    | {
        titleLead?: T;
        titleAccent?: T;
        titleRest?: T;
        description?: T;
        actions?:
          | T
          | {
              label?: T;
              href?: T;
              variant?: T;
              opensLeadForm?: T;
              analyticsId?: T;
              id?: T;
            };
      };
  applicationsSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        image?: T;
        alt?: T;
        items?:
          | T
          | {
              title?: T;
              description?: T;
              id?: T;
            };
      };
  layerSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        accent?: T;
        cards?:
          | T
          | {
              eyebrow?: T;
              title?: T;
              description?: T;
              id?: T;
            };
      };
  licenseeSection?:
    | T
    | {
        title?: T;
        description?: T;
        logos?:
          | T
          | {
              label?: T;
              image?: T;
              slug?: T;
              id?: T;
            };
      };
  seo?:
    | T
    | {
        title?: T;
        description?: T;
        image?: T;
        noindex?: T;
      };
  _status?: T;
  updatedAt?: T;
  createdAt?: T;
  globalType?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "ora_select".
 */
export interface OraSelect<T extends boolean = true> {
  heroSection?:
    | T
    | {
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        image?: T;
        alt?: T;
        callout?:
          | T
          | {
              title?: T;
              description?: T;
            };
        action?:
          | T
          | {
              label?: T;
              href?: T;
              variant?: T;
            };
      };
  enablesSection?:
    | T
    | {
        image?: T;
        alt?: T;
        enabled?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        cards?:
          | T
          | {
              title?: T;
              description?: T;
              id?: T;
            };
      };
  seo?:
    | T
    | {
        title?: T;
        description?: T;
        image?: T;
        noindex?: T;
      };
  _status?: T;
  updatedAt?: T;
  createdAt?: T;
  globalType?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "solutions_select".
 */
export interface SolutionsSelect<T extends boolean = true> {
  heroSection?:
    | T
    | {
        titleLead?: T;
        titleAccent?: T;
        titleRest?: T;
        description?: T;
        note?: T;
        actions?:
          | T
          | {
              label?: T;
              href?: T;
              variant?: T;
              opensLeadForm?: T;
              analyticsId?: T;
              id?: T;
            };
      };
  panels?:
    | T
    | {
        eyebrow?: T;
        titlePrefix?: T;
        title?: T;
        titleAccent?: T;
        description?: T;
        capabilities?:
          | T
          | {
              text?: T;
              id?: T;
            };
        id?: T;
      };
  partnerSection?:
    | T
    | {
        eyebrow?: T;
        titleLead?: T;
        titleAccent?: T;
        description?: T;
        accent?: T;
        accentTarget?: T;
        breakAfterLead?: T;
        items?:
          | T
          | {
              title?: T;
              description?: T;
              id?: T;
            };
      };
  seo?:
    | T
    | {
        title?: T;
        description?: T;
        image?: T;
        noindex?: T;
      };
  _status?: T;
  updatedAt?: T;
  createdAt?: T;
  globalType?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "support_select".
 */
export interface SupportSelect<T extends boolean = true> {
  heroSection?:
    | T
    | {
        title?: T;
        description?: T;
        action?:
          | T
          | {
              label?: T;
              href?: T;
              variant?: T;
            };
      };
  qrCards?:
    | T
    | {
        label?: T;
        description?: T;
        qrImage?: T;
        icon?: T;
        id?: T;
      };
  contactSection?:
    | T
    | {
        enabled?: T;
        title?: T;
      };
  seo?:
    | T
    | {
        title?: T;
        description?: T;
        image?: T;
        noindex?: T;
      };
  _status?: T;
  updatedAt?: T;
  createdAt?: T;
  globalType?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "navigation_select".
 */
export interface NavigationSelect<T extends boolean = true> {
  header?:
    | T
    | {
        label?: T;
        url?: T;
        activePathPrefix?: T;
        newTab?: T;
        children?:
          | T
          | {
              label?: T;
              url?: T;
              newTab?: T;
              id?: T;
            };
        id?: T;
      };
  footer?:
    | T
    | {
        tagline?: T;
        columns?:
          | T
          | {
              title?: T;
              links?:
                | T
                | {
                    label?: T;
                    url?: T;
                    newTab?: T;
                    id?: T;
                  };
              id?: T;
            };
        legalLinks?:
          | T
          | {
              label?: T;
              url?: T;
              newTab?: T;
              id?: T;
            };
        copyright?: T;
      };
  updatedAt?: T;
  createdAt?: T;
  globalType?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "settings_select".
 */
export interface SettingsSelect<T extends boolean = true> {
  demoBookingUrl?: T;
  storeUrl?: T;
  setupGuideUrl?: T;
  overviewVideoUrl?: T;
  salesEmail?: T;
  supportEmail?: T;
  officeAddress?: T;
  legalEntityName?: T;
  logo?: T;
  defaultShareImage?: T;
  defaultSeoTitle?: T;
  defaultSeoDescription?: T;
  canonicalOrigin?: T;
  updatedAt?: T;
  createdAt?: T;
  globalType?: T;
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "collections_widget".
 */
export interface CollectionsWidget {
  data?: {
    [k: string]: unknown;
  };
  width: 'full';
}
/**
 * This interface was referenced by `Config`'s JSON-Schema
 * via the `definition` "auth".
 */
export interface Auth {
  [k: string]: unknown;
}


declare module 'payload' {
  export interface GeneratedTypes extends Config {}
}